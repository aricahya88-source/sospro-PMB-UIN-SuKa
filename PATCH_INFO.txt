PATCH ADMIN AUTH FINAL V4

Masalah yang ditangani:
- Endpoint /exec aktif, tetapi admin masih menerima "Akses admin ditolak."
- Pesan generik tersebut menunjukkan deployment Apps Script masih menjalankan Code.gs versi lama.
- Patch menambahkan normalisasi trim() pada secret dan pesan diagnostik yang lebih spesifik.
- doGet() kini mengembalikan version:"auth-v4" agar deployment aktif dapat diverifikasi.
- lib/gas.ts mempertahankan timeout 45 detik untuk cold start Apps Script.

FILE GOOGLE APPS SCRIPT (WAJIB):
apps-script/Code.gs
1. Copy seluruh isinya ke editor Google Apps Script yang terikat ke spreadsheet.
2. Save.
3. Deploy > Manage deployments > Edit (ikon pensil) > Version: New version > Deploy.
4. Buka GAS_ENDPOINT /exec di browser.
5. Pastikan JSON memuat: "version":"auth-v4".

FILE GITHUB:
lib/gas.ts
Replace file ini di GitHub, commit, dan tunggu Vercel redeploy.

PENTING:
- Jangan menaruh API_SECRET di GitHub.
- GAS_SECRET di Vercel harus sama persis dengan API_SECRET di Script Properties.
- Jika /exec belum menampilkan version auth-v4, berarti GAS_ENDPOINT menunjuk deployment lama atau deployment belum diperbarui.
