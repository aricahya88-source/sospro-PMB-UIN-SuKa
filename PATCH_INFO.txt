PATCH V3 — Apps Script lambat / timeout 15 detik

Gejala yang ditangani:
- URL GAS_ENDPOINT /exec akhirnya menampilkan {"ok":true,"message":"Sospro API aktif"}, tetapi lambat.
- Admin menampilkan "Koneksi ke Apps Script melebihi 15 detik".

Perubahan:
1. lib/gas.ts
   - Timeout dinaikkan dari 15 detik menjadi 45 detik.
   - Pesan error diperjelas.
   - Tidak melakukan retry otomatis pada operasi tulis agar tidak berisiko membuat data ganda.
2. Semua API route yang berbicara ke Apps Script diberi maxDuration = 60 detik.

File yang harus direplace di GitHub:
- lib/gas.ts
- app/api/admin/events/route.ts
- app/api/admin/attendance/route.ts
- app/api/admin/draw/route.ts
- app/api/public/events/route.ts
- app/api/public/attendance/route.ts

Tidak perlu mengubah Apps Script Code.gs untuk patch ini.
Tidak perlu mengubah Environment Variables.
Setelah commit, tunggu Vercel redeploy sampai Ready lalu buka /admin/kegiatan kembali.
